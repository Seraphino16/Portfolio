services:
  portfolio_app:
    container_name: portfolio
    build:
      context: ./
      target: prod
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - 3000:3000